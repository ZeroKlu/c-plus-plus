#include <iostream>

int main() {
    std::cout << "Hello World!" << std::endl; // Applies the namespace to the call rather than a "using" (and uses << endl instead of \n)
    std::cout << "I am learning C++";
    
    return 0;
}