#include <iostream>             // IO Header library (provides "cout" below)
using namespace std;            // Exposes the standard library

int main() {                    // Main executable function
    cout << "Hello World!\n";   // Uses the insertion operator (<<) to output text
    cout << "I am learning C++";
    
    return 0;                   // End the main function
}